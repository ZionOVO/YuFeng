# AGENTS.md

本文件是本仓库的开发规范，对人工智能代理与人类同等生效。动手前先读完本文；架构问题以 [`docs/architecture.md`](docs/architecture.md) 为唯一权威。

---

## 0. 项目速览

御锋 2.0：智能代理编排的安全修复系统。架构一句话：**一个中台 · 两类边缘 · 五种契约；智能在中台，设备上只有哑执行器；数据制品承载一切易变物。**

- 平台二进制：`yufeng-brain`（后端控制面）、`yufeng-edge`（数据面）、`yufeng-host`（资产侧）、`yufeng-jarvis`（独立编排智能代理进程）、`yufeng-agentd`（执行实例监督进程）、`yufeng-run`（短命执行智能代理）和 `yufeng-dataplane`（本机数据面监督器），外加命令行工具 `yfctl`；智能代理进程与中台只走网络契约；贾维斯不持 Docker；
- 技术选型状态以 architecture.md §7 为准：当前基座为 Go 1.27.0、Connect 远程过程调用框架、Protocol Buffers 消息契约与 Buf 工具链、内嵌 NATS 消息服务器、PostgreSQL 数据库与 pgx 驱动、goose 数据库迁移工具；sqlc 生成层尚无业务调用方；Coraza 仅检测模式已引入，引擎命中本身不返回拒绝响应。控制台正式采用 Tailwind CSS 与 HeroUI；Connect-ES（Connect 协议的 TypeScript 客户端生成与运行库）已选定，但当前交付也允许手写 Connect JSON 调用；
- 硬约束：`edge` 与 `host` 不使用 C 语言互操作；补丁知识写进 `procedures/` 数据制品，**禁止写进平台代码**；完整负面清单见 architecture.md §11。流量拦截层的生产语义以 [`docs/design.md`](docs/design.md) 第 4 节为准（检测键、检查覆盖度、资产世代、形状语言、Inspector、Gate、入口姿态）；第一阶段正则闭环只是演示。仓库目标版本只读取根目录 [`VERSION`](VERSION)，对外已发布状态只读取 [GitHub Releases](https://github.com/ZionOVO/YuFeng/releases/latest)；只有同名非草稿 Release 的精确提交证据资产复核通过，才可宣称该版本的软件发布与机器验收闭环。客户上线仍要求现场负责人填写真实代理网段、上游、证书、网络核对结果和变更责任人；证据合同见 [`docs/delivery-evidence.md`](docs/delivery-evidence.md)。Python 不得进平台二进制；可选中台亲和异步检测见 architecture.md 的架构决策记录 027。

## 1. 设计模式策略（已定稿，不再讨论）

**正统极简为底、类型驱动上身、管道只进两处。**

| 子系统 | 模式 | 要点 |
|---|---|---|
| `lib/kernel`（制品生命周期、RepairPlan） | 类型驱动：类型状态机 | 状态是类型：`Draft.Sign()→Signed`、`Shadow.Promote()→Canary`；单机单元不足时另有合法边 `Shadow.PromoteEnforce()→Enforce`（`docs/api.md` §7.6），不是非法转换 |
| `agents/runtime`（统一智能代理运行时中的执行实例） | 类型驱动：补偿事务 | 步骤 = 命令对象（参数 + 守卫 + 补偿动作）；每步回执、失败走补偿分支 |
| `edgecore` 决策 | 函数式：纯函数内核 | `Inspect` / `Gate` 类决策函数不执行输入输出、不读取时钟、不写日志；壳层薄。活路径若仍名 `Evaluate` 且返回 `Action`，应拆成只输出发现的检查器与只依据已签名策略裁决的闸门 |
| 检测器 / 角色 / 工具等扩展点 | 极简：小接口 + 注册表 | `Inspector` 一个方法最好、无 `Action`；注册即插拔；闸不进注册表 |
| `edgecore` 请求路径 | 管道：中间件链 | 仅此一处用链 |
| 遥测 / 脱敏 / 重试 | 管道：装饰器 | 横切面包装，不侵入业务 |
| 装配 / 配置 / 错误 | 极简：从简 | 普通结构体配置；入口函数手工装配；不用函数式选项、不用依赖注入框架 |

**反模式（代码评审直接打回）**：为每个结构体预配接口（接口定义在消费者侧）、函数式选项链、单例与全局可变状态、依赖注入框架、为未来预建抽象（重复三次才抽象）、横幅式分隔注释、注释掉的代码。

## 2. 开发流程

1. **动手顺序**：改网络应用程序编程接口（API）的行为或状态语义先改 `docs/api.md`，再改 `proto/`；改数据消息字段或编码先改 `proto/`，再同步文档；两者冲突时阻止合入并人工对齐。改架构先改 `docs/architecture.md`（必要时增加架构决策记录）→ 改行为先改测试 → 最后写实现。
2. **提交前**：`make build test vet` 全绿；`gofmt` 已格式化；不引入未说明的新依赖（新依赖需在提交说明里给一句理由，优先标准库）。
3. **提交规范**：Conventional Commits——`type(scope): 中文一句话`。type 用 `feat/fix/refactor/docs/test/chore`；scope 用顶层目录名（kernel/edgecore/agents/…）。一次提交只做一件事，不混重构与功能。
4. **分支与合并**：`main`、`develop` 双主干加工作分支，一律通过拉取请求合并，见下节分支模型。
5. **契约产物**：buf 生成代码入库（保证 `go build` 不依赖 buf 工具链）；`buf breaking` 不过不许合入。
6. **测试要求**：决策类纯函数必须有表驱动测试；移植/回放语义的模块必须带对比测试（输入→期望输出成对）；新包至少有一个能跑通的测试文件占位可后补，但行为代码不得无测试合入。
7. **任务收尾**（代理特别注意）：报告里如实写明跑了哪些验证、哪些没做；不确定的设计问题停下来问，不要自行发明架构。
8. **语义化定位**（见下节）：实施计划、代码与产品文档一律使用完整行为名称，并通过路径、符号和产品文档章节定位。

### 语义化命名与定位

文件名、目录名、条目标题、符号和注释必须直接表达职责。数据库迁移序号、协议版本、架构决策记录号、文档章节号与修复连续谱层级具有稳定定义，可以保留；任何必须依赖旧清单才能解释的编号都应改写为完整语义。

**禁止**把不透明编号或任务短键写进：

- 源文件名、测试文件名、包名、目录名；
- 函数、类型、变量、常量、测试函数、`describe` / `it` 标题；
- 代码注释；行为缺口必须写成当前事实、失败边界和可验证的期望；
- 产品文档与说明；
- 测试里对产品文档的锁句。

实施计划的每个条目都写明相关文档章节、源文件与符号。实现时按这些定位全仓追踪消息、字段和消费者，不靠把计划标题烙进代码反查。

有明确定义的数字包括文档章节、架构决策记录号、修复连续谱层级、数据库迁移顺序、协议版本和线上枚举值。首次使用修复连续谱层级或协议枚举时，必须链接术语表或接口契约。

| 错 | 对 |
|---|---|
| `g0_kind_test.go` | `propose_intent_reject_test.go` |
| `// 处理边缘情况` | `// 生产提案缺少意图或携带演示规则时必须失败关闭。` |
| 架构文档写「见第二批」 | 写 `ModelGatewayService.CompleteChat`（`docs/api.md` 第 19.4 节） |
| 对话里说「做完设置项」 | 说「完成控制台 `/app/setup` 六步引导」 |

新增或改写 [`implementation-plan.md`](implementation-plan.md) 条目必须带定位和验收。删掉标题后，单靠定位仍应能找到要改的文档章节、源文件与符号。

### 分支模型

| 分支 | 用途 |
|---|---|
| main | 线上版本，随时可发布 |
| develop | 日常开发集成 |
| release/vX.Y.Z | 从冻结 `develop` 创建的发布稳定分支，集中修复并承载一次完整发布预检 |
| feature/* | 新功能 |
| fix/* | 普通 bug 修复 |
| spec/* | 跨文档或跨模块的规格校准 |

**基本规则**：

1. 禁止直接推送 `main` 或 `develop`，必须走拉取请求；
2. 拉取请求审批后方可合并；
3. `main` 只接受同一仓库的 `develop` 分支；发布门禁必须确认 `main` 相对双方最近共同基线没有额外内容改动，并确认最终 `develop` Git 树等于发布预检 Git 树、精确提交持续集成成功；
4. 工作分支拉取请求进入 `develop` 时只运行构建、普通单元测试、格式化、综合静态检查、消息契约、控制台、交叉编译和必要的受影响模块快速门禁；不在日常拉取请求运行完整竞态、漏洞、活栈、备份恢复和五场景性能；
5. 准备发布时从冻结的 `origin/develop` 创建 `release/vX.Y.Z`。发布问题只在该分支修复；最终合并前在临时工作树对预期合并 Git 树完整运行一次本机静态预检：完整竞态、`yufeng_dev` 构建标签、格式化、综合静态分析、漏洞、消息契约、控制台、交叉编译、Apple M4 Pro 热路径基准和秘密扫描。已被完整竞态包含的普通测试、生产端到端、故障注入和 `*-live.sh static` 只供定向复跑，不再重复执行；
6. 发布稳定分支本机静态预检和远端快速门禁成功后，只通过一次拉取请求以合并提交进入 `develop`。最终 `develop` 持续集成只确认精确合并提交、Git 树、两个父提交和已成功的发布分支拉取请求，不运行测试套件。随后本机证据提升验证同一 Git 树、两个父提交、工具链与环境指纹、预检有效期和远端结果，只补一次 Docker 活栈、备份恢复、五场景每秒 2000 请求性能与秘密扫描，再装配归档；不得重跑静态套件，也不得通过反复合入 `develop` 刷新证据；
7. `develop` 合入 `main` 时不重复完整持续集成，只运行发布来源、主干内容和已有证据检查；必须使用合并提交，使 `develop` 已验提交作为发布提交的父提交被完整保留；
8. `main` 每次合并后必须立即创建递增的 `vX.Y.Z` 发布版本标签；标签必须指向当时最新的 `main` 提交，并由标签工作流构建、校验和发布制品；标签不是代码第一次接受测试的入口；
9. feature / fix / spec 拉取请求合入 `develop` 后，当次删除远端头分支；发布稳定分支在证据提升完成后删除；`develop` 合入 `main` 的发布拉取请求不得删除 `develop`；
10. 除 `main` 与 `develop` 外，工作分支允许直接推送；任何工作分支合入 `develop` 仍必须走拉取请求并通过检查。

**远端托管限制**：当前私有仓库的 GitHub 账户计划不支持分支保护或规则集，平台不能把必需检查设为不可绕过的合并锁。上述工作流仍会给出明确的成功或失败证据；在仓库升级到支持分支保护的计划或改为公开仓库前，合并人必须等待对应工作流成功，并把任何失败或未完成状态视为禁止合并。不得把这项人工纪律描述成平台已经强制执行。

**日常流程**：

- 做功能：从 `develop` 拉 feature 分支 → 推送工作提交 → 提拉取请求到 `develop` → 快速门禁与审批通过后合并 → 删除工作分支；
- 校准规格：从 `develop` 拉 spec 分支 → 推送工作提交 → 提拉取请求到 `develop` → 快速门禁与审批通过后合并 → 删除工作分支；
- 发版：冻结 `develop` → 创建 `release/vX.Y.Z` 并集中修复 → 对预期合并 Git 树完成一次本机静态预检 → 远端快速门禁确认后只合入 `develop` 一次 → 等待最终 `develop` 精确提交确认成功 → 对同一 Git 树本机运行一次活栈、恢复与性能并提升证据 → `develop` 向 `main` 提发布拉取请求 → 发布门禁与审批通过后使用合并提交 → 创建注解标签 → 标签工作流生成 Draft Release 与制品 → 上传三项本机证据 → 远端复核后公开 Release。

## 3. 代码风格

- 标识符英文，注释中文；错误信息英文小写、不以标点结尾（符合 Go 惯例、grep 友好）；
- 命名：短、达意；包名小写单词；**禁止** `util`、`common`、`helper`、`misc` 包；
- 接口：定义在消费者侧，返回值用具体类型（"accept interfaces, return structs"）；
- 错误：`errors.Is/As` 判断，`fmt.Errorf("...: %w", err)` 包装；禁止吞错与 `_ =` 忽略返回错误（除非显式注明原因）；
- 并发：`context.Context` 永远是第一个参数；并发编排用 errgroup；禁止包级可变全局；
- 泛型：只用于注册表、容器等真正参数化的场景；
- 魔法数字：出现在两处以上即命名常量。

## 4. 注释格式（严格执行）

**总则：一句话定义 + 行为约束，名词的完整解释不放注释**——放 [docs/glossary.md](docs/glossary.md)，注释里用 Go 原生文档链接引用（`go doc` / gopls / pkg.go.dev 直接渲染，无需第三方框架）。不遗留版本号等历史痕迹。写法：

```go
// Claims 是能力令牌的声明结构（令牌的载荷部分）。
//
// 能力令牌约束四件事：
//   - 是谁：Subject 与 Role 声明智能代理身份与角色；角色只决定默认权限，实际授权以令牌为准；
//   - 能用哪些工具：Tools 白名单之外的调用一律拒绝；
//   - 能用多少次：MaxCalls 预算，每次调用记账，超限即拒；
//   - 只能碰哪些对象：Bindings 圈定数据范围，工具的返回与引用不得越界。
//
// [能力令牌]: ../../docs/glossary.md#capability-token
```

链接目标用相对路径，**以源文件所在目录为基准数层数**（本例 Claims 在 `lib/kernel/`，到 `docs/` 需要两层 `../../`；`cmd/<命令>/` 同为两层；仓库根目录文件一层）。层数算错，链接就解析到不存在的路径——这是易错点，写完自查。锚点用术语表的英文标识。

**层级一 · 包注释**：每个包一个 `doc.go`（或置于首个文件顶部），一句话说清职责：

```go
// Package kernel 实现治理内核：制品签名与内容地址、能力令牌、发布生命周期状态机与门禁判据。
package kernel
```

**层级二 · 导出符号注释**：以标识符名开头，第一句说"做什么"，需要时第二段说"何时用/约束"。不写"是什么"的废话：

```go
// SignArtifact 用私钥对制品签名，返回带签名的制品副本。
// 参与签名的字节范围见 Artifact.SignedBytes。
func SignArtifact(a Artifact, key ed25519.PrivateKey) (Artifact, error)
```

**层级三 · 行内注释**：只解释**为什么**（约束、陷阱、取舍），不复述代码在做什么；代码能说清的不写。关键不变量写在类型或函数声明上方：

```go
// 回执必须先于副作用落盘：先记账后执行，否则崩溃窗口内会出现
// 无凭据的副作用；回执丢失视为步骤失败，进补偿分支。
```

**层级四 · 已知缺口**：不使用任务标签。注释只写当前限制、触发条件和失败边界；需要排期的工作在 [`implementation-plan.md`](implementation-plan.md) 用完整语义标题、定位和验收记录：

```go
// 沙箱演练目前仅覆盖 Debian 软件包；遇到 RPM 软件包管理器格式时失败关闭。
```

**禁止**：注释掉的代码、变更日志式注释（"modified by / 2026-08-14 改"）、banner 分隔线（`//----`）、与代码矛盾的过时注释（改代码必同步改注释）。

## 5. 目录速查

```
cmd/          入口：edge / brain / host / jarvis / agentd / yufeng-run / yufeng-dataplane / yfctl
lib/          edgecore · kernel · brain · edgeclient · replay · eventbus · store · observability
agents/       runtime 已建；roles / tools / skills 预留；modelgateway 是客户端与假提供者，生产聊天出口在 brain 的 `ModelGatewayService.CompleteChat`
components/   intelligence / evaluation 预留；modelside 为可选独立 Python 进程（不进平台二进制）
proto/        契约（buf）
procedures/   程序制品包预留（当前仅 steps.schema.json）
bpf/          内核侧 BPF 预留（当前无 C 源或对象）
console/ deploy/ docs/
```

## 6. 沟通与术语规范

**规则**（对提问、汇报、评审意见、提交说明、文档、与用户的对话全部生效）：

1. 禁止裸缩写与自造简称。缩写首次出现必须带全称和一句话解释，之后同一篇内可用简称；
2. 禁止用不透明阶段号或任务短键代替功能名。要么写清行为与路径，要么引用实施计划中的完整条目标题和定位；
3. 固定术语以 [docs/glossary.md](docs/glossary.md) 为准；新术语先进术语表（含英文锚点）、再使用；
4. 与用户对话同样生效，不许默认对方记得缩写含义。

术语表正文已独立为 [docs/glossary.md](docs/glossary.md)（含稳定锚点，供代码注释链接）。
